<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:m="clr-namespace:UraniumUI.Icons.MaterialSymbols;assembly=UraniumUI.Icons.MaterialSymbols"
             xmlns:converters="clr-namespace:IVCNetMaui.Converters"
             xmlns:viewModels="clr-namespace:IVCNetMaui.ViewModels.View"
             xmlns:controls="clr-namespace:IVCNetMaui.Controls"
             x:Class="IVCNetMaui.Controls.CameraControl"
             x:Name="This"
             x:DataType="viewModels:CameraControlViewModel">
    <ContentView.Resources>
        <ResourceDictionary>
            <Style TargetType="Picker">
                <Setter Property="FontSize" Value="10"/>
                <Setter Property="BackgroundColor" Value="#9021252A" />
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="WidthRequest" Value="120" />
            </Style>
            <converters:ToPickerItemConverter x:Key="ToPickerItemConverter" />
        </ResourceDictionary>
    </ContentView.Resources>
    
    <Grid ColumnDefinitions="Auto, *, Auto" RowDefinitions="Auto, *, Auto" BackgroundColor="Black">
        <Image Source="camera_placeholder.png" Grid.ColumnSpan="3" Grid.RowSpan="3" >
            <Image.GestureRecognizers>
                <TapGestureRecognizer Tapped="ControlGrid_Tapped"/>
            </Image.GestureRecognizers>
        </Image>
        
        <!-- Header Controls -->
        <Grid x:Name="HeaderControls" Grid.ColumnSpan="3" ColumnDefinitions="Auto, *, Auto" Padding="4" Opacity="{Binding ControlOpacity}">
            <HorizontalStackLayout Spacing="6">
                <Picker Title="Select Unit"
                        ItemsSource="{Binding Units}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource ToPickerItemConverter}}"
                        SelectedIndex="{Binding SelectedUnitIndex, Mode=TwoWay}"/>
                <Picker Title="Select Feed"
                        ItemsSource="{Binding Feeds}"
                        ItemDisplayBinding="{Binding ., Converter={StaticResource ToPickerItemConverter}}"
                        SelectedIndex="{Binding SelectedFeedIndex, Mode=TwoWay}"/>
            </HorizontalStackLayout>
            <controls:IconButtonControl Grid.Column="2" Glyph="{x:Static m:MaterialSharp.Close}" Command="{Binding DeleteCommand}" />
        </Grid>
        
        <!-- Bottom Left Controls -->
        <HorizontalStackLayout x:Name="BottomLeftControls" Grid.Column="0" Grid.Row="2" Padding="4, 4" Spacing="6" VerticalOptions="End" Opacity="{Binding ControlOpacity, Mode=TwoWay}">
            <controls:IconButtonControl IsCheckable="True" 
                                        IsChecked="{Binding PtzIsVisible}" 
                                        Glyph="{x:Static m:MaterialSharp.Control_camera}"
                                        Command="{Binding TogglePtzCommand}"/>
            <controls:IconButtonControl Glyph="{x:Static m:MaterialSharp.Photo_camera_back}" />
            <controls:IconButtonControl Glyph="{x:Static m:MaterialSharp.Video_camera_back}" />
        </HorizontalStackLayout>
        
        <!-- Bottom Right Controls -->
        <Grid x:Name="BottomRightControls" Grid.Row="2" Grid.Column="2" ColumnDefinitions="Auto, Auto, Auto, Auto" RowDefinitions="Auto, Auto, Auto" IsVisible="{Binding PtzIsVisible}" Padding="4" Opacity="{Binding ControlOpacity, Mode=TwoWay}">
            <controls:IconButtonControl Grid.Row="1" Glyph="{x:Static m:MaterialSharp.Keyboard_arrow_left}" />
            <controls:IconButtonControl Grid.Column="1" Glyph="{x:Static m:MaterialSharp.Keyboard_arrow_up}" />
            <controls:IconButtonControl Grid.Column="2" Grid.Row="1" Glyph="{x:Static m:MaterialSharp.Keyboard_arrow_right}" />
            <controls:IconButtonControl Grid.Row="2" Grid.Column="1" Glyph="{x:Static m:MaterialSharp.Keyboard_arrow_down}" />
            <controls:IconButtonControl Grid.Row="0" Grid.Column="3" Glyph="{x:Static m:MaterialSharp.Add}" />
            <controls:IconButtonControl Grid.Row="2" Grid.Column="3" Glyph="{x:Static m:MaterialSharp.Remove}" />
        </Grid>
    </Grid>
</ContentView>
